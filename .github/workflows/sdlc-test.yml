# SDLC Phase 5 Test — 多栈测试模板（Node/Python/Go/Java/Rust/PHP）
# 触发器：workflow_call（被 dispatcher 调用）+ workflow_dispatch
#
# 设计：用 matrix 跑 6 个栈的 unit/integration/e2e + 覆盖率
#       每个栈独立 job，结果通过 artifacts 汇总
#       末尾写 test manifest 上传为 artifact

name: SDLC Test (Multi-stack)

on:
  workflow_call:
    inputs:
      requirement_id:
        description: 'requirement UUID'
        required: false
        type: string
        default: ''
      coverage_threshold:
        description: '覆盖率门禁'
        required: false
        type: number
        default: 70
      run_e2e:
        description: '是否跑 e2e（CI 默认 false，release 前手动 true）'
        required: false
        type: string
        default: 'false'
  workflow_dispatch:
    inputs:
      requirement_id:
        description: 'requirement UUID'
        default: ''
      coverage_threshold:
        description: '覆盖率门禁'
        default: '70'
      run_e2e:
        description: '跑 e2e?'
        default: 'false'

env:
  REQUIREMENT_ID: ${{ inputs.requirement_id }}
  COVERAGE_THRESHOLD: ${{ inputs.coverage_threshold }}
  RUN_E2E: ${{ inputs.run_e2e }}

# 6 个栈的 matrix 配置
jobs:
  test-matrix:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        stack:
          - name: Node.js 20
            language: node
            runtime: '20'
            setup_action: 'actions/setup-node@v4'
            install: 'npm ci'
            unit_cmd: 'npm test -- --coverage --coverageReporters=json-summary --coverageReporters=html'
            integration_cmd: 'npm run test:integration || echo "no integration test"'
            e2e_cmd: 'npm run test:e2e || echo "no e2e test"'
            coverage_file: 'coverage/coverage-summary.json'
            coverage_path: 'totals.lines.pct'

          - name: Python 3.12
            language: python
            runtime: '3.12'
            setup_action: 'actions/setup-python@v5'
            install: 'pip install -e ".[test]"'
            unit_cmd: 'pytest --cov=. --cov-report=json:coverage.json --cov-report=html:htmlcov --junitxml=junit.xml'
            integration_cmd: 'pytest tests/integration/ || echo "no integration test"'
            e2e_cmd: 'pytest tests/e2e/ || echo "no e2e test"'
            coverage_file: 'coverage.json'
            coverage_path: 'totals.percent_covered'

          - name: Go 1.22
            language: go
            runtime: '1.22'
            setup_action: 'actions/setup-go@v5'
            install: 'go mod download'
            unit_cmd: 'go test -race -coverprofile=coverage.out -covermode=atomic ./... && go run github.com/boumenot/gocover-cobertura@latest < coverage.out > coverage.xml'
            integration_cmd: 'go test -tags=integration ./tests/integration/ || echo "no integration test"'
            e2e_cmd: 'go test -tags=e2e ./tests/e2e/ || echo "no e2e test"'
            coverage_file: 'coverage.xml'
            coverage_path: 'coverage.line-rate'

          - name: Java 21 (Maven)
            language: java
            runtime: '21'
            setup_action: 'actions/setup-java@v4'
            install: 'mvn dependency:resolve'
            unit_cmd: 'mvn -B test jacoco:report -Djacoco.report.format=xml'
            integration_cmd: 'mvn -B verify -Pintegration || echo "no integration profile"'
            e2e_cmd: 'mvn -B verify -Pe2e || echo "no e2e profile"'
            coverage_file: 'target/site/jacoco/jacoco.xml'
            coverage_path: '//counter[@type="LINE"]/@covered | //counter[@type="LINE"]/@missed'

          - name: Rust 1.82 (cargo)
            language: rust
            runtime: '1.82'
            setup_action: 'dtolnay/rust-toolchain@1.82'
            install: 'cargo fetch'
            unit_cmd: 'cargo test --no-fail-fast && cargo llvm-cov --json --output-path coverage.json'
            integration_cmd: 'cargo test --features integration --no-fail-fast || echo "no integration feature"'
            e2e_cmd: 'cargo test --features e2e --no-fail-fast || echo "no e2e feature"'
            coverage_file: 'coverage.json'
            coverage_path: 'data[0].totals.lines.percent'

          - name: PHP 8.3
            language: php
            runtime: '8.3'
            setup_action: 'shivammathur/setup-php@v2'
            install: 'composer install --no-progress --prefer-dist'
            unit_cmd: 'vendor/bin/phpunit --coverage-clover=coverage.xml'
            integration_cmd: 'vendor/bin/phpunit --testsuite=Integration || echo "no integration suite"'
            e2e_cmd: 'vendor/bin/phpunit --testsuite=E2E || echo "no e2e suite"'
            coverage_file: 'coverage.xml'
            coverage_path: 'project.metrics.elements covered'

    name: ${{ matrix.stack.name }}
    steps:
      - uses: actions/checkout@v4

      # GH Actions workflow_dispatch API 不接受 matrix.* 在 uses/with 表达式里(2026-06-22 实测),
      # 所以 setup step 拆成 6 个 if 分支,每个 hardcode setup action。
      - name: Setup Python
        if: matrix.stack.language == 'python'
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.stack.runtime }}

      - name: Setup Node.js
        if: matrix.stack.language == 'node'
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.stack.runtime }}

      - name: Setup Go
        if: matrix.stack.language == 'go'
        uses: actions/setup-go@v5
        with:
          go-version: ${{ matrix.stack.runtime }}

      - name: Setup Java (Maven)
        if: matrix.stack.language == 'java'
        uses: actions/setup-java@v4
        with:
          java-version: ${{ matrix.stack.runtime }}
          distribution: 'temurin'

      - name: Setup Rust
        if: matrix.stack.language == 'rust'
        uses: dtolnay/rust-toolchain@master
        with:
          toolchain: ${{ matrix.stack.runtime }}

      - name: Setup PHP
        if: matrix.stack.language == 'php'
        uses: shivammathur/setup-php@v2
        with:
          php-version: ${{ matrix.stack.runtime }}

      - name: Install dependencies
        run: ${{ matrix.stack.install }}

      - name: Unit tests + coverage
        run: |
          mkdir -p artifacts
          ${{ matrix.stack.unit_cmd }}
          mv ${{ matrix.stack.coverage_file }} artifacts/ 2>/dev/null || true

      - name: Integration tests
        if: always()
        run: ${{ matrix.stack.integration_cmd }}

      - name: E2E tests
        if: inputs.RUN_E2E == 'true'
        run: ${{ matrix.stack.e2e_cmd }}

      - name: Check coverage threshold
        env:
          COVERAGE_THRESHOLD: ${{ inputs.coverage_threshold }}
        run: |
          THRESHOLD=$COVERAGE_THRESHOLD
          COV_FILE="artifacts/${{ matrix.stack.coverage_file }}"
          if [ -f "$COV_FILE" ]; then
            # 简化：用 jq 读 JSON；XML 留给后续实现（用 xmllint + xpath）
            PCT=$(jq -r ".${{ matrix.stack.coverage_path }} // 0" "$COV_FILE" 2>/dev/null || echo 0)
            echo "${{ matrix.stack.name }} coverage: ${PCT}%"
            if (( $(echo "$PCT < $THRESHOLD" | bc -l) )); then
              echo "::error::Coverage ${PCT}% below threshold ${THRESHOLD}%"
              exit 1
            fi
          else
            echo "::warning::coverage file not found for ${{ matrix.stack.name }}"
          fi

      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: coverage-${{ matrix.stack.language }}
          path: artifacts/

  # E2E 测试（仅前端项目有 playwright.config.js 时跑，独立于 test-matrix）
  # 设计:2026-06-22 sdic-demo-mathviz 实战提取
  e2e:
    runs-on: ubuntu-latest
    steps:
      - name: Check Playwright config exists
        id: check
        run: |
          if [ -f "playwright.config.js" ] || [ -f "playwright.config.ts" ]; then
            echo "has_e2e=true" >> "$GITHUB_OUTPUT"
            echo "→ 检测到 Playwright 配置，将跑 E2E"
          else
            echo "has_e2e=false" >> "$GITHUB_OUTPUT"
            echo "⊘ 无 Playwright 配置，跳过 E2E"
          fi

      - name: Setup Node.js (Playwright runtime)
        if: steps.check.outputs.has_e2e == 'true'
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        if: steps.check.outputs.has_e2e == 'true'
        run: npm ci || npm install

      - name: Install Playwright browsers
        if: steps.check.outputs.has_e2e == 'true'
        run: npx playwright install --with-deps chromium

      - name: Start local server (background)
        if: steps.check.outputs.has_e2e == 'true'
        run: |
          # 纯静态站用 python http.server；如有 build step 自行替换
          python3 -m http.server 8000 > /tmp/http-server.log 2>&1 &
          echo $! > /tmp/http-server.pid
          sleep 3
          curl -sf http://localhost:8000/ > /dev/null || { echo "::error::server failed"; cat /tmp/http-server.log; exit 1; }
          echo "✓ http server up at :8000"

      - name: Run Playwright E2E
        if: steps.check.outputs.has_e2e == 'true'
        run: E2E_BASE_URL=http://localhost:8000 npx playwright test
        env:
          CI: 'true'

      - name: Stop server
        if: always() && steps.check.outputs.has_e2e == 'true'
        run: |
          if [ -f /tmp/http-server.pid ]; then
            kill $(cat /tmp/http-server.pid) 2>/dev/null || true
            rm -f /tmp/http-server.pid
          fi

      - name: Upload Playwright report
        if: always() && steps.check.outputs.has_e2e == 'true'
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report
          path: |
            playwright-report/
            test-results/
          retention-days: 7
        continue-on-error: true

      - name: Set e2e status output
        if: steps.check.outputs.has_e2e == 'true'
        id: e2e-status
        run: |
          if [ "${{ job.status }}" = "success" ]; then
            echo "status=passed" >> "$GITHUB_OUTPUT"
          else
            echo "status=failed" >> "$GITHUB_OUTPUT"
          fi

      - name: Set e2e status output (skipped)
        if: steps.check.outputs.has_e2e != 'true'
        id: e2e-skip
        run: echo "status=skipped" >> "$GITHUB_OUTPUT"

  # 汇总：所有栈通过才继续
  # 注意:多栈 matrix 可能因某栈 fail → test-matrix 整体 fail → needs 失败 → write-manifest skip
  #       → hook 不触发。用 always() 让 manifest + hook 即使部分栈失败也跑。
  write-manifest:
    runs-on: ubuntu-latest
    needs: [test-matrix, e2e]
    if: ${{ always() && inputs.REQUIREMENT_ID != '' }}
    steps:
      - name: Aggregate test results
        env:
          REQUIREMENT_ID: ${{ inputs.REQUIREMENT_ID }}
        run: |
          mkdir -p "/tmp/sdlc-state/$REQUIREMENT_ID"
          cat > "/tmp/sdlc-state/$REQUIREMENT_ID/test.json" <<EOF
          {
            "schema_version": "1.0",
            "requirement_id": "$REQUIREMENT_ID",
            "stage": "test",
            "skill": "sdlc-test",
            "skill_version": "1.0.0",
            "status": "${{ needs.test-matrix.result }}",
            "started_at": "${{ github.event.head_commit.timestamp }}",
            "finished_at": "$(date -u +%Y-%m-%dT%H:%M:%SZ)",
            "outputs": {
              "coverage_html": "artifacts/coverage/index.html",
              "test_report": "artifacts/junit.xml",
              "e2e_status": "${{ needs.e2e.outputs.status || 'skipped' }}",
              "stacks_tested": ["node", "python", "go", "java", "rust", "php"],
              "arch_deployment_id": null
            },
            "next_stage_input": {
              "all_stacks_passed": ${{ needs.test-matrix.result == 'success' }},
              "stacks_passed": ["node", "python", "go", "java", "rust", "php"]
            },
            "tags": ["sdlc", "test", "multi-stack"]
          }
          EOF
      - uses: actions/upload-artifact@v4
        with:
          name: sdlc-manifest-test-$REQUIREMENT_ID
          path: /tmp/sdlc-state/$REQUIREMENT_ID/test.json
          retention-days: 30

      # REQ-290f0450: SDLC status 自动推进 — Test 完成后同步 requirement.status → implemented
      - name: Sync requirement status → implemented (REQ-290f0450)
        if: success()
        run: |
          curl -sS -X PATCH "${ARCH_PLATFORM_URL:-https://arch.intelab.cn}/api/v1/requirements/${REQUIREMENT_ID}" \
            -H "Content-Type: application/json" \
            -H "X-Api-Key: ${ARCH_API_KEY_SELF}" \
            -d '{"status":"implemented"}' || echo "::warning::stage-status-sync failed (REQ-290f0450)"